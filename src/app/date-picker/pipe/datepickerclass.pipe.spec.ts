import { DatePickerClassPipe } from './datepickerclass.pipe';

describe('DatePickerClassPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePickerClassPipe();
    expect(pipe).toBeTruthy();
  });
});
